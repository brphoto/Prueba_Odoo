<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_customer_experience_metrics" model="ir.cron">
        <field name="name">Experiencia: recalcular LTV, NPS y segmentos</field>
        <field name="model_id" ref="model_crm_experience_snapshot"/>
        <field name="state">code</field>
        <field name="code">model.cron_refresh()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + relativedelta(days=1)).strftime('%Y-%m-%d 02:30:00')"/>
    </record>
    <record id="ir_cron_customer_experience_invitations" model="ir.cron">
        <field name="name">Experiencia: preparar NPS para facturas pagadas</field>
        <field name="model_id" ref="model_crm_nps_invitation"/>
        <field name="state">code</field>
        <field name="code">model._cron_create_paid_invitations()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + relativedelta(days=1)).strftime('%Y-%m-%d 03:00:00')"/>
    </record>
</odoo>
