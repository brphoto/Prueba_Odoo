<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_transaction_form_datafast" model="ir.ui.view">
        <field name="name">payment.transaction.form.datafast</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/field[@name='state']" position="before">
                <button
                    type="object"
                    name="action_datafast_verify"
                    string="Verificar en Datafast"
                    class="oe_highlight"
                    invisible="provider_code != 'datafast' or state in ('done', 'cancel')"/>
            </xpath>
        </field>
    </record>
</odoo>
